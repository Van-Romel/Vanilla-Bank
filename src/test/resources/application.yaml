server:
  port: 8081

spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:vanilla_bank_db_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL
    username: root
    password:
    driver-class-name: org.h2.Driver
springdoc:
  api-docs:
    path: /api-docs
